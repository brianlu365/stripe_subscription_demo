<%= current_user.email %>
<% if @subscription.active %>
  
  subscribed
  <h3>current plan renew at: <%= Time.at(@stripe_subscription[:current_period_end]).strftime("%m/%d/%y") %></h3>
  <%= link_to 'Cancel Subscription', users_cancel_subscription_path %>
<% else %>
  <%= render 'form' %>
<% end %>
